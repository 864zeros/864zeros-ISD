# 864zeros: Factory Manifest [v1.9]

**Authority:** Per-extension monetization-readiness ledger. Synthesizes the post-consolidation state of all 15 extensions in `LLC-DIV-3-FACTORY/extensions/`.
**Loaded:** On demand for tier-rollout planning, GTM sequencing, RULE-001 burndown, and operator dashboards.
**Authored:** 2026-05-09 by 864z-OA (Office Architect) per RULE-000.
**Update protocol:** Append a new version after each strike that changes the Tier-0.5 readiness of any extension OR after each new extension joins the factory.
**Sources synthesized:** All 15 `extensions/*/manifest.json` + `_locales/{locale}/messages.json` + `options/*` files (programmatic survey 2026-05-09 post-Strike-014 transparency-consolidation pass) · [`864zeros_TECH_STACK_AUDIT.md`](./864zeros_TECH_STACK_AUDIT.md) (class taxonomy + RULE compliance lens) · [`864zeros_PILLAR_STRATEGY.md`](./864zeros_PILLAR_STRATEGY.md) (pillar doctrine).
**Format note:** Follows the `864z-markdown-standard` (RULE-008).

---

## I. Naming Convention Note

**Per RULE-006 v1.1 the codified pillar tags are:** `[OIA]`, `[864F]`, `[FHG]`. The brand identifier `864-Flux` (full pillar name) appears in the standardized 4-line brand-footer (per GTM_MANIFEST §6) and elsewhere in marketing surfaces. The bracketed tag `[864F]` is the RULE-006 v1.1 enforced form for `manifest.json.name` and rendered surface pills. **`[FLUX]` is not a codified tag** — informal references to "Flux" should normalize to either `[864F]` (in code) or `864-Flux` (in copy).

---

## II. Fleet at a Glance

**12 active extensions** across 3 pillars (post-Strike-016 archival: 3 legacy `oia.focus.*` extensions moved to `_archive/`; TabVault rebranded to DataNap). Tier-0.5 readiness distribution snapshot (2026-05-09 post-Strike-021):

| Status | Count | Extensions |
|---|---|---|
| **🚀 RUNG 4: ACTIVE CHECKOUT** (real ExtPay payment flow) | **1** | `864z-chronicle` ⬆ promoted in Strike 024 (real ExtPay; stub retired) |
| **✅ TIER-0.5 SHIPPED (Rung 4 — stub paywall)** | **0** ⬇ from 1 (chronicle promoted out) |
| **🏆 SCAFFOLD-READY (Rung 3+ — markup wired + state machine + dev gate + canonical IDs)** | **11** | `Bible-Insight`, `clipboard` ⬆ promoted in Strike 021, `DataNap`, `Focus Note`, `Focus Wall`, `migration-pilot`, `scripture-scout`, `Signal2Noise`, `Time2Focus`, `TuneOut2FocusIn`, `who-is-watching` |
| **🟡 SCAFFOLD-READY (CSS only)** | **0** ✅ |
| **❌ BLOCKED** (no `options_ui` → RULE-001 violation) | **0** ✅ |

**Strike-019 milestones:**
- Bible-Insight RULE-007 §Disclosure block injected (closes the one outstanding P1 from Strike 018 audit; Bible-Insight now fully RULE-007 §Disclosure compliant)
- migration-pilot + scripture-scout promoted Rung 2 → Rung 3 via `lib/tier.js` distribution + inline tier-init script injection
- **11 of 12 active extensions now on Rung 3+ (92%)** — only `clipboard` remains on Rung 1 (Phase-2 HIGH-deferred)
- NEW operational artifact: `LLC-DIV-3-FACTORY/FACTORY_LEDGER.jsonl` (machine-readable JSON-per-line) + `LLC-DIV-3-FACTORY/SESSION_STREAM.md` (human-readable companion) — append-only audit trail of every atomic factory mutation

**Strike-020 milestones (visual-compliance + GTM lock-in):**
- 🏆 **OIA (ADHD) pillar: 8/8 Rung-3+ extensions are now visual-binding compliant** — every OIA extension's options page has the canonical `vault-tier-card` / `current-tier-name` / `vault-lock-watermark` IDs that the inline tier-init script targets. 100% visual-compliance.
- 🏆 **FHG pillar: 2/2 Rung-3+ extensions are now visual-binding compliant** — Bible-Insight + scripture-scout both have canonical IDs. 100% visual-compliance.
- **864-Flux: 1/2** — migration-pilot now visual-compliant (Strike 020 canonical IDs added); clipboard still Rung 1 (Phase-2 HIGH-deferred).
- Bible-Insight tier rebranded **"⌖ Tier-0.5: Vault" → "⌖ Sovereign Research Kit"** per Operator GTM decision; closes Strike-018 P0 GTM-decision item. $2.99 perpetual unlock model adopted (matches Chronicle pattern).

**🏆🏆🏆 Strike-021 FLEET-WIDE 100% milestone:**
- **clipboard Phase-2 closure**: clipboard already had the [864F] prefix + SW `type: "module"` + brand-footer + linked `lib/transparency-tier.css` from prior strikes (Strike 014 transparency consolidation + earlier scaffold work). Strike 021 added the missing pieces: `lib/tier.js`, canonical IDs (`vault-tier-card` / `current-tier-name` / `vault-lock-watermark`), Tier-0.5 "Sovereign History" LOCKED card with $2.99 perpetual messaging, dev-override panel + inline tier-init script.
- **clipboard RULE-007 audit delivered**: per-extension `RULE_007_AUDIT.md` (~16 KB; 8 sections). Verdict: ✅ STRUCTURALLY COMPLIANT. Covers BYOK Gemini + debugger bounded to PDF + chrome.identity Google OAuth (BYOA) + ExtPay 3rd-party (NOT a 864zeros proxy) + chrome.management bounded to extension enumeration.
- **🏆 ACTIVE 12-EXTENSION FLEET: 12/12 (100%) Rung 3+ + 12/12 (100%) visual-binding compliant** — every extension in every pillar is now at minimum Rung 3 with full canonical-ID visual binding. Chronicle still holds the only Rung-4 (SHIPPED with stub paywall) position. The Tier-0.5 readiness ladder is no longer a per-extension shortlist; it is now a fleet-wide property.
- 864-Flux pillar: 2/2 visual-compliant (was 1/2). All 3 pillars at 100% visual-compliance.

**🔐 Strike-029 milestones (TRUST VAULT UI FLEET ROLLOUT):**
- **Canonical `lib/options-tier-init.js` gains `injectTrustVaultUI()`** — renders operator-mandated UI into any `#trust-vault-root` element. Idempotent via `root.dataset.injected='029'`. Five operator-verbatim text artifacts present byte-exact: Header `864zeros Trust Vault`; Status Label `Privacy Status: Local-Only (No Data Stored by 864zeros)`; Export button `Export Data Backup`; Import button `Import Data Backup`; Privacy Guarantee block (3-point list closing with "You own the keys. You own the data. You own the vault."). App name in filename extracted from `chrome.runtime.getManifest().name` with `[PILLAR]` prefix stripped.
- **Default button behavior**: Export = `chrome.storage.local.get(null)` → `exportVault(appName, data)` → `.md` download. Import = `importVault()` (which shows operator-verbatim alert from Strike 028) → on data return, `chrome.storage.local.clear()` + `chrome.storage.local.set(data)` (OVERWRITE semantics per warning text) → re-renders tier card, dev label, brand mission.
- **Trust Vault library now fleet-wide** — `lib/trust-vault.js` distributed from chronicle-only (Strike 028) to all 12 extensions. Sha `935bc37a...` identical across canonical + 12 copies. The 11 extensions previously without the library now have functional Trust Vault buttons.
- **24-file fleet redistribution**: both updated `options-tier-init.js` (sha `ad42cff6...`) AND `trust-vault.js` (sha `935bc37a...`) distributed to all 12 active extensions' `lib/`. 24/24 SHA-identical to canonicals; 24/24 module-mode syntax-clean.
- **12 options.html updated** — `<div id="trust-vault-root"></div>` (with Strike-029 marker comment) inserted immediately before the standardized brand-footer in all 12 active extensions. Python script handled 3 anchor-pattern variations (9 ext use `class="brand-footer"`, 3 ext use `class="brand-footer oia-mt-md"`); Bible-Insight's non-standard `html/options.html` path handled via candidate-list iteration. 12/12 final verification. Page order in chronicle (the most-built extension): dev-override-panel → brand-mission → **trust-vault-root** → brand-footer — same insertion point as the other 11.
- No Rung promotions this strike (UI rollout work, not Rung advance); active fleet remains 12/12 Rung 3+ with chronicle on 🚀 Rung 4: Active Checkout. **Fleet-wide Trust Vault parity: 0 / 12 → 12 / 12.**

**🛡 Strike-027 milestones (PAYMENT-PERSISTENCE REPAIR + 4TH DEFENSE LAYER):**
- **Wrapper race closed**: `extpay-wrapper.js initPayments()` refactored to accept an optional `onPaidCallback` parameter and register the listener BEFORE `extpay.startBackground()` polls. The Strike-026 diligence had noted this race ("ExtPay's first poll could fire between startBackground() and a separately-deferred addListener() call, causing a missed payment-confirmation event on SW restart when the user had already paid in a prior session") and explicitly chose not to fix it; Strike 027 closes it. The exported `onPaid()` is retained for backwards-compatibility + secondary listeners.
- **SW switched to single-call pattern**: `initPayments(async (user) => {...})` replaces the prior `initPayments(); onPaid(handler);` two-call sequence. One import (initPayments only), one call site. Race window eliminated structurally.
- **NEW canonical fail-safe `tryExtPaySync()`** in `lib/options-tier-init.js` — dynamic-imports `./payments/extpay-wrapper.js` (try/catch — graceful no-op for the 11 extensions without a wrapper); calls `mod.getCurrentTier()`; if remote='vault' and local!='vault', calls `setTier(TIER_VAULT)` + direct `renderTier() + refreshDevTierLabel()` re-render. **Upgrade-only this strike** (downgrade / refund handling is its own concern per `CHRONICLE_CHECKOUT_BLUEPRINT.md` §V FM4). Wired on (a) page init (after renderTier/initDevOverride/injectBrandMission) AND (b) inside the existing `window.focus` listener (alongside renderTier+refreshDevTierLabel).
- **Cross-fleet distribution**: updated canonical redistributed to all 12 extensions; 12/12 SHA-identical (sha `2b7b3012...`); 12/12 module-mode syntax-clean. The 11 extensions without an `extpay-wrapper.js` will have `tryExtPaySync()` silently no-op via the dynamic-import catch — no errors thrown, no log spam. When their own wrappers land per blueprint §VI generalization, the fail-safe activates automatically.
- **Defense-in-depth payment-confirmation chain now has 4 layers** (in order of expected firing):
  1. SW `onPaid()` callback (Strike 024 + 027 race-closure) — primary path; fires when ExtPay's content-script-back-to-SW message arrives.
  2. `chrome.runtime.sendMessage` TIER_UNLOCKED (Strike 024) — Options tab open AND listening at broadcast time.
  3. `window.focus` listener (Strike 026) — Options tab was offscreen at broadcast; user returns from Stripe.
  4. `tryExtPaySync()` on init + focus (Strike 027) — **ExtPay as source of truth**; catches everything the above three missed (SW evicted mid-payment, transient race, no broadcast received).
  5. (chronicle-only bonus) `chrome.storage.onChanged 'tier'` (Strike 024) — fires on ANY setTier write, including dev-override panel clicks.
- No Rung promotions this strike (persistence + safety-net work, not Rung advance); active fleet remains 12/12 Rung 3+ with chronicle on 🚀 Rung 4: Active Checkout. Payment-success path is now structurally race-free + observably resilient.

**🔧 Strike-026 milestones (CHRONICLE PAYMENT-FLOW DEBUG + UX HARDENING):**
- **Chronicle SW `onPaid()` handler instrumented** — 4-point diagnostic logging (callback fired with email + paid status; setTier committed; TIER_UNLOCKED broadcast outcome; redirect attempt outcome). Try/catch around setTier (early return on persistence failure — do NOT broadcast TIER_UNLOCKED if storage write didn't land); try/catch around sendMessage (rejects when no Options tab listening — not fatal). Operator can verify each step in chrome://extensions DevTools for the SW.
- **Best-effort "redirect" back to options.html on payment success** — storage-passed tab ID approach (no new manifest permission). Options page `onUnlockVault()` writes its tab ID to `chrome.storage.local.paymentReturnTabId` BEFORE opening checkout; SW reads it on payment success and calls `chrome.tabs.update(id, {active: true}) + chrome.windows.update(windowId, {focused: true})`. ExtPay SDK verified to lack any native redirect param (open_payment_page only takes plan_nickname); this is the code-side substitute. Manifest's permissions array unchanged (still just `storage + sidePanel` — chrome.tabs.update with a known ID does NOT require `tabs` permission).
- **Canonical `lib/options-tier-init.js` gains a window-focus listener** — `window.addEventListener('focus', () => { renderTier(); refreshDevTierLabel(); })`. Covers the manual-return-from-Stripe case where the runtime.sendMessage may have fired (and missed) before the user clicked back to the options tab. Redistributed to all 12 extensions (12/12 SHA-identical, module-mode syntax-clean). Belt-and-suspenders alongside `chrome.runtime.onMessage` (Strike 024) and `chrome.storage.onChanged` (chronicle-only, Strike 024).
- **Chronicle options.js `TIER_UNLOCKED` listener now shows visible confirmation** — split TIER_UNLOCKED vs TIER_DOWNGRADED branches; on UNLOCKED: `toast('Payment Successful! Vault unlocked.', 5000)`; on DOWNGRADED: `toast('Vault locked — payment status changed.', 5000)`. 5-second duration so the user has time to read it.
- **Three-layer defense** for the "user returns to options tab and sees the new tier" UX: (1) SW-side broadcast catches the case where the tab is open AND listening; (2) shared focus listener catches the case where the tab was open but offscreen during broadcast; (3) chronicle's chrome.storage.onChanged listener catches the case where the tier flag changed but no broadcast was received (e.g., SW evicted mid-payment). All three signals are idempotent; safe to all fire together.
- No Rung promotions this strike (debug + UX hardening); active fleet remains 12/12 Rung 3+ with chronicle on 🚀 Rung 4: Active Checkout. Chronicle's payment-success path is now observable + resilient.

**🎯 Strike-025 milestones (CROSS-SURFACE BRAND_MISSION INSTALLATION):**
- **NEW canonical `864z-build-kit/references/core/brand-identity.js`** — single source of truth for cross-surface 864zeros brand copy. Exports `BRAND_MISSION` constant (operator-verbatim founder-voice statement, 4 sentences, em-dash preserved). Future canonical brand strings (tagline, attestations, pillar slogans) can layer on top.
- **Canonical `lib/options-tier-init.js` updated** — imports `BRAND_MISSION`; defines `injectBrandMission()` (defensive null-check on `#brand-mission` element); calls it after `initDevOverride()`. Pages without the div no-op (idempotent).
- **24-file fleet-wide redistribution** — both `brand-identity.js` (NEW, 12 copies) and updated `options-tier-init.js` (12 copies) distributed to all 12 active extensions' `lib/` directories. 24/24 SHA-identical to canonicals; 24/24 module-mode syntax-clean. The 11 other extensions get the BRAND_MISSION injection for free as their options.html files add the `<div id="brand-mission">` element.
- **Chronicle's `options/options.html` gains the `<div id="brand-mission" class="brand-text"></div>` div** — first extension to render the BRAND_MISSION. Placed above the standardized 4-line brand-footer (sits as a brand-manifesto layer between the dev-override-panel and the legal footer). `.brand-text` class unstyled by default; styling deferred for operator iteration.
- **NEW canonical template at `ISD-DIV-5-EVOLUTION/templates/GTM_BUILD_REPORT_TEMPLATE.md`** — fulfills the per-strike Build Report mandate from `ROLES/OFFICE_ARCHITECT.md` §IV (previously unimplemented). BRAND_MISSION as blockquote header (lockstep with `brand-identity.js`); 8 RULE-008-compliant sections (Thesis · Target Customer · Source Liberation Targets · Pricing Tier · Hook Copy · Privacy & RULE-007 Disclosure · Cross-References · Versioning). Becomes the source-of-truth template for all future `BR-{strike-id}-{codename}.md` Build Report instances.
- No Rung promotions this strike (branding/copy work, not tier-readiness); active fleet remains 12/12 Rung 3+ with chronicle on 🚀 Rung 4: Active Checkout.

**🚀 Strike-024 milestones (REVENUE ARC OPENED — first real-payment extension live):**
- **Chronicle ExtPay implementation SHIPPED per `CHRONICLE_CHECKOUT_BLUEPRINT.md`** — all 3 entry points wired: (a) SW `initPayments() + onPaid()` handler in `service-worker.js` (sets `TIER_VAULT` + broadcasts `TIER_UNLOCKED`); (b) Options page `onUnlockVault()` swap from Strike-013 stub to `extpay.openPaymentPage()`; (c) shared `TIER_UNLOCKED`/`TIER_DOWNGRADED` listener appended to canonical `lib/options-tier-init.js` + redistributed to all 12 extensions (one-line addition benefits future per-extension ExtPay rollouts for free).
- **Chronicle promoted Rung 4 (stub) → Rung 4 (Active Checkout)** — first revenue-generating extension live in the fleet. Stub-unlock CTA + two-tap arm retired (ExtPay's checkout page IS the confirmation gate).
- **NEW gitignored config artifact**: `extensions/864z-chronicle/js/config.js` — operator-supplied `EXTPAY_ID` (merchant slug) + `SOVEREIGN_PRICE_ID` (Stripe price ID metadata) + `PLAN_ID` (internal plan label). Excluded from version control via `.gitignore`. Only `EXTPAY_ID` flows into the SDK call; the other two are operator metadata for Stripe-dashboard cross-reference.
- **NEW chronicle infrastructure**: `lib/payments/ExtPay.js` (vendored 3rd-party SDK, identical SHA to clipboard's copy + canonical) + `lib/payments/extpay-wrapper.js` (chronicle-specific minimal wrapper with binary tier model — simpler than clipboard's 4-tier wrapper); manifest.json updated with `content_scripts` matching `https://extensionpay.com/*` (per ExtPay's documented setup pattern).
- **Chronicle migrated to shared `lib/options-tier-init.js`** (closes Strike-022 P1 — chronicle was the lone holdout). Chronicle's `initDevOverride()` and `refreshDevTierLabel()` deleted from `options.js` (handled by shared script); `renderTierUI()` retained for chronicle-extras (description text + markdown button); two new listeners added (`chrome.runtime.onMessage` for TIER_UNLOCKED + `chrome.storage.onChanged` for any tier-flag write). 418 LOC final (down from 443; net -25). **12/12 fleet now uses shared tier-init.**
- **Vendored canonical `ExtPay.js` to `864z-build-kit/references/core/payments/`** (closes Strike-022 P2). Single source of truth for the SDK across the fleet; future per-extension payment integrations sync from this canonical.
- Active fleet: **12/12 Rung 3+ unchanged** (chronicle was already Rung 4-stub; this is a within-rung promotion to Active Checkout state). The 11 other Rung-3 extensions remain Rung 3+ pending their own ExtPay rollouts (per blueprint §VI generalization, ~5.5h batched).

**Strike-022 milestones (consolidation + payment-architecture spec):**
- **clipboard §Disclosure injected** (closes Strike-021 P1) — verbatim from `clipboard/RULE_007_AUDIT.md §VI.a`; covers all 5 high-trust surfaces (AI BYOK / Google OAuth / ExtPay 3rd-party / debugger / management) in clipboard's Options page.
- **NEW canonical: `864z-build-kit/references/core/options-tier-init.js`** — extracts the inline `<script type="module">` tier-init pattern (Strikes 016/017/018/019/021) into a single shared module. Distributed to per-extension `lib/options-tier-init.js` across **11 extensions** (chronicle excluded — its tier-init lives in `options/options.js`, not inline; touching it risks the reference impl).
- **11 extensions migrated from inline-script to shared-script linkage** — `<script type="module">{...}</script>` blocks replaced with `<script type="module" src="../lib/options-tier-init.js"></script>`. Eliminates ~80 LOC of cross-extension code duplication; future updates to tier-init logic now touch ONE canonical file instead of 11.
- **Chronicle Checkout Blueprint authored** (`extensions/864z-chronicle/CHRONICLE_CHECKOUT_BLUEPRINT.md`, ~15 KB / 203 lines, RULE-008 compliant; 8 sections). Identifies 3 ExtPay entry points (SW `initPayments+onPaid`, Options page CTA swap, shared `TIER_UNLOCKED` broadcast listener), 6 operator pre-integration checklist items, 6 failure modes + mitigations, generalization path for the other 11 Rung-3 extensions (~5.5h batched estimate). NOT an implementation — operator-gated.

**Fleet compliance milestone (Strike 017):**
- **RULE-001 (Cog-triggered Options page): 12 / 12 (100%)** — was 9/15 pre-Strike-016
- **RULE-006 v1.1 (`extName` pillar prefix): 12 / 12 (100%)** — was 1/15 pre-Strike-014
- **SW `type: "module"` modernization: 12 / 12 (100%)** — was 14/15 pre-Strike-017 (`who-is-watching` was the lone classic-script SW)

Pillar distribution: **OIA 8 · 864-Flux 2 · FHG 2** (per `864zeros_PILLAR_STRATEGY.md` §VI — pillar inventory now reflects post-archival state).

---

## III. Per-Extension Manifest

12 active extensions (3 archived in Strike 016 — see [`extensions/_archive/README.md`](../../864zeros-llc/LLC-DIV-3-FACTORY/extensions/_archive/README.md)).

| Extension | Pillar | Display name (current) | Version | Options page | Tier-0.5 Status |
|---|---|---|---|---|---|
| `864z-chronicle` | OIA | `[OIA] Chronicle` | 1.1.0 | ✅ `options/options.html` | **🚀 RUNG 4: ACTIVE CHECKOUT** — Strike 024 promotion: real ExtPay checkout flow live (stub retired). 3 entry points wired per `CHRONICLE_CHECKOUT_BLUEPRINT.md`: SW `initPayments + onPaid` (sets TIER_VAULT + broadcasts TIER_UNLOCKED); Options `openPaymentPage()`; shared `TIER_UNLOCKED` listener in canonical `lib/options-tier-init.js` (redistributed to all 12). NEW `lib/payments/{ExtPay.js, extpay-wrapper.js}` + gitignored `js/config.js` (operator-supplied EXTPAY_ID + SOVEREIGN_PRICE_ID + PLAN_ID). Migrated to shared `lib/options-tier-init.js` (closes Strike-022 P1; was the lone holdout). manifest.json gains `content_scripts` for `extensionpay.com/*` per ExtPay's setup pattern. Pre-existing Strike 013 features intact: tier card + `lib/tier.js` + Sovereign Link header button + RULE-005 two-tap Clear All + ?dev=1 dev-override panel + DEV_NOTES.md. **First revenue-generating extension in the fleet.** |
| `Bible-Insight` | FHG | `[FHG] Bible Insight` | 1.0.0 | ✅ `html/options.html` | ✅ **Rung 3 — VISUAL-COMPLIANT + RULE-007 §Disclosure compliant + tier rebranded "Sovereign Research Kit"** — Strike 020 closed Strike-018 P0 GTM-decision item: tier renamed `⌖ Tier-0.5: Vault` → `⌖ Sovereign Research Kit`; $2.99 perpetual unlock (Chronicle pattern); 5-feature list (Sovereign Link Backup, Markdown vault export, PDF report archive, cross-translation diffing, all-future-features); "why $2.99 once" rationale. CTA: "Unlock Sovereign Research Kit — $2.99 (coming soon)". Internal tier flag name unchanged (TIER_VAULT) for cross-extension code consistency. |
| `clipboard` | 864-Flux | `[864F] ClipBoard` | 1.0.0 | ✅ `options/options.html` | ✅ **Rung 3+ — VISUAL-COMPLIANT + RULE-007 §Disclosure compliant** — Strike 021 Phase-2 closure (lib/tier.js + Sovereign History card + canonical IDs + dev-override panel + audit doc). **Strike 022**: RULE-007 §Disclosure block injected before brand-footer (verbatim from `RULE_007_AUDIT.md §VI.a`; covers all 5 high-trust surfaces — AI BYOK Gemini / Google Drive OAuth / ExtPay payments / debugger / management). Inline tier-init script extracted to shared `lib/options-tier-init.js` (Strike 022 fleet consolidation; 11 extensions). |
| `DataNap` (was TabVault) | OIA | `[OIA] DataNap` | 1.0.0 | ✅ `options/options.html` | 🟢 **SCAFFOLD-READY** — Strike 016 augmentation. Web Store listing update pending the rebrand. |
| `migration-pilot` | 864-Flux | `[864F] MigrationPilot — Web to Obsidian` | 0.1.0 | ✅ `options/options.html` | ✅ **Rung 3 — VISUAL-COMPLIANT (state machine + binding)** — Strike 020: canonical IDs (`vault-tier-card` / `current-tier-name` / `vault-lock-watermark`) added to first existing `tier-card--locked` div (the "Pro coming" card). Closes the Strike-019 visual-binding partial-state. Card content preserved; ID layer purely additive; renderTier() now toggles visual state when dev gate flips chrome.storage.local.tier. |
| `oia-focus-note` (Focus Note) | OIA | `[OIA] Focus Note` | 1.1.0 | ✅ `options/options.html` (Strike 016) | 🟢 **SCAFFOLD-READY** — full RULE-001 + Tier-0.5 + dev-override scaffold. |
| `oia-focus-wall` (Focus Wall) | OIA | `[OIA] Focus Wall` | 1.1.0 | ✅ `options/options.html` (Strike 016) | 🟢 **SCAFFOLD-READY** — full RULE-001 + Tier-0.5 + dev-override scaffold. |
| `scripture-scout` | FHG | `[FHG] ScriptureScout` | 0.1.0 | ✅ `options/options.html` | ✅ **Rung 3 — VISUAL-COMPLIANT (state machine + binding)** — Strike 020: canonical IDs added to first existing `tier-card--locked` div (mirror change to migration-pilot; templates were forked from same source). Closes the Strike-019 visual-binding partial-state. Founding-100 cohort gates real launch. |
| `Signal2Noise` | OIA | `[OIA] Signal2Noise` | 1.0.0 | ✅ `options/options.html` | 🟢 **SCAFFOLD-READY** — Strike 016 augmentation. |
| `Time2Focus` | OIA | `[OIA] Time2Focus` | 1.0 | ✅ `options/options.html` | 🟢 **SCAFFOLD-READY** — Strike 016 augmentation. |
| `TuneOut2FocusIn` | OIA | `[OIA] TuneOut2FocusIn` | 1.0.0 | ✅ `options/options.html` | 🟢 **SCAFFOLD-READY** — Strike 016 augmentation. |
| `who-is-watching` | OIA | `[OIA] Who Is Watching` | 2.1.6 | ✅ `options/options.html` (Strike 017) | 🟢 **SCAFFOLD-READY** — Strike 017 scaffold + SW `type: "module"` migration in same touch. Highest-version extension in fleet. |

**Archived (3 — Strike 016):** `oia.focus.signal`, `oia.focus.sound`, `oia-focus-timer` — frozen; not part of active fleet. See `_archive/README.md`.

---

## IV. Tier-0.5 Readiness Ladder

A four-rung ladder describing what each extension needs to advance to the next tier of Tier-0.5 readiness. Lower rungs are prerequisites for higher rungs.

### IV.a — Rung 0: Cog-triggered Options page (RULE-001)

**Prerequisite for ANY Tier-0.5 work.** An extension cannot host a tier disclosure until it has an `options_ui` page conforming to RULE-001's three mandatory sections (How to Use · Subscription & Tiers · Data Management).

**0 extensions on Rung 0** ✅ — fully cleared as of Strike 017 (`who-is-watching` was the last; closed in Strike 017 alongside the SW `type: "module"` migration). Rung-0 is now an empty bucket; the historical 6 entries (5 `oia.focus.*` Focus-class + `who-is-watching`) closed across Strikes 016 and 017.

### IV.b — Rung 1: Transparency baseline (CSS + brand-footer + brand-prefix pill)

Extension has the shared `lib/transparency-tier.css` linked + the standardized 4-line brand-footer rendered + the RULE-006 v1.0 `.brand-prefix` pill in surface headers. **Visual contract for tier disclosure is in place** even if the actual tier card hasn't been built yet.

**12 of 12 active extensions on Rung 1+** ✅ (every active extension after Strikes 014-017).

### IV.c — Rung 2: Tier-card markup wired

Extension's options.html has `<div class="tier-card tier-card--locked">...</div>` markup with the canonical structure (`.tier-card__head`, `.tier-card__name`, `.tier-card__price`, `.tier-features`, `.tier-card__cta`, `.tier-card__lock-watermark`).

**11 of 12 active extensions on Rung 2+** (everything except `clipboard`, which uses a different pre-Tier-0.5 paid-tier ladder; full migration is HIGH-deferred Phase 2).

Strike 018 closed the `tier-card--upcoming` legacy-variant gap: `migration-pilot` and `scripture-scout` now use the canonical `tier-card--locked` class name. Their dead local `--upcoming` CSS rules were replaced with marker comments pointing to `../lib/transparency-tier.css` for the canonical styling.

### IV.d — Rung 3: Tier state machine + locked/unlocked logic

Extension has `lib/tier.js` (or equivalent) exporting `getTier()` / `setTier()` / `isVaultUnlocked()`. Options-page JS reads tier on load and toggles `.tier-card--locked` ↔ `.tier-card--unlocked` accordingly.

**🏆 12 of 12 active extensions on Rung 3+** (post-Strike-021 — fleet-wide 100% milestone): `864z-chronicle` (Rung 4 — Strike 013 reference) + the 7 Strike-016/017 cohort (DataNap, Focus Note, Focus Wall, Signal2Noise, Time2Focus, TuneOut2FocusIn, who-is-watching) + `Bible-Insight` (Strike 018) + `migration-pilot` + `scripture-scout` (Strike 019) + `clipboard` (Strike 021 Phase-2 closure).

**Rung 0/1/2 are now empty buckets** for the active fleet. The next ladder advance is Rung 3 → Rung 4 (paywall + dev-override + DEV_NOTES) — that's a per-extension upgrade with cumulative effort but no longer a structural compliance gap.

### IV.e — Rung 4: Paywall + dev-override + DEV_NOTES

Extension has a stub or real payment-processor integration on the unlock CTA + the `?dev=1` URL-gated dev-override panel + a `DEV_NOTES.md` documenting both. **This is the "Tier-0.5 SHIPPED" tier.**

**Strike 024 introduces a sub-rung distinction:**

- **Rung 4 (Active Checkout)** — real payment-processor integration live; checkout actually charges customers; SW `onPaid()` handler observes payments and persists tier. **1 extension**: `864z-chronicle` ⬆ promoted Strike 024.
- **Rung 4 (stub)** — Strike-013-style paywall placeholder (UI says "Unlock $X" but the click flips the tier flag locally without payment). Useful for shipping the UX before payment infrastructure lands. **0 extensions** ⬇ from 1 (chronicle vacated).

**8 extensions are at "Rung 4 minus payment integration"** — they have the dev-override panel + URL gate + tier-state machine, but lack the production unlock CTA wiring. Each needs ~30 min to replicate Chronicle's Active-Checkout pattern (per `CHRONICLE_CHECKOUT_BLUEPRINT.md` §VI generalization path); the operator-side ExtPay merchant registration is a separate per-extension call.

---

## V. Recommended Strike Sequence (priority-ordered, post-Strike-024)

> 🚀 **REVENUE ARC OPENED:** Chronicle is the first revenue-generating extension in the fleet (Strike 024 — real ExtPay live). The next P0 fans the same pattern out to Bible-Insight + clipboard (the 2 other extensions with their own named tiers — Sovereign Research Kit + Sovereign History). After those land, the remaining 9 Rung-3 extensions can roll out ExtPay in a single batched strike (~4.5h) per `CHRONICLE_CHECKOUT_BLUEPRINT.md` §VI generalization.

| Priority | Strike candidate | Effort | Unlocks |
|---|---|---|---|
| **🔥 P0-TOP** | **Bible-Insight ExtPay integration (Sovereign Research Kit) + clipboard ExtPay integration (Sovereign History)** — replicate Chronicle's Strike-024 pattern: per-extension `lib/payments/{ExtPay.js, extpay-wrapper.js}` + gitignored `js/config.js` + SW `initPayments + onPaid` + Options page CTA swap. Shared `TIER_UNLOCKED` listener already in canonical `lib/options-tier-init.js` (Strike 024). | ~30 min × 2 = ~1h batched | Promotes Bible-Insight (Sovereign Research Kit) + clipboard (Sovereign History) from Rung-3 → Rung-4 (Active Checkout); **3 of 12 active extensions live with real payment** |
| **P1** | DataNap Web Store listing update (rebrand publish) | ~1h | Operator-side marketing; required pre-publish |
| **P1** | Per-extension RULE-007 audit follow-up for Chronicle (post-ExtPay) — add §V (ExtPay 3rd-party) parallel to clipboard's audit; verdict expected: structurally compliant since pattern matches clipboard's already-audited integration | ~30 min | Closes the Strike-024 audit-loop; matches clipboard's per-extension audit pattern |
| **P1** | Chronicle privacy-disclosure block — add ExtPay 3rd-party paragraph to `options/options.html` Privacy section (verbatim from clipboard's §Disclosure block, Strike 022) | ~15 min | Brings chronicle to RULE-007 §Disclosure parity with clipboard |
| **P2** | Replicate ExtPay integration across the remaining 9 Rung-3 extensions (per blueprint §VI) | ~30 min × 9 = ~4.5h batched | Promotes 9 extensions from Rung-3 → Rung-4 (Active Checkout) — fleet-wide revenue-active milestone |
| **P3** | ScriptureScout pre-flight scarcity OR (DIV-1 Live Scout) | ~1-2h | Operator-driven competitive recon; charter held in BACKLOG.md |
| ~~🔥 P0-TOP~~ | ~~Chronicle ExtPay implementation per `CHRONICLE_CHECKOUT_BLUEPRINT.md`~~ | — | ✅ **CLOSED in Strike 024** (3 entry points wired; Chronicle promoted Rung 4-stub → Rung 4-Active-Checkout) |
| ~~P1~~ | ~~Migrate chronicle's `options.js` tier-init logic to use shared `lib/options-tier-init.js`~~ | — | ✅ **CLOSED in Strike 024** (12/12 fleet now uses shared tier-init) |
| ~~P2~~ | ~~Vendor canonical `ExtPay.js` SDK to `864z-build-kit/references/core/payments/`~~ | — | ✅ **CLOSED in Strike 024** (single source of truth for the SDK across the fleet) |
| ~~P0~~ | ~~All prior Rung-0/1/2 closures + Bible-Insight audit + Bible-Insight §Disclosure + canonical-IDs + Bible-Insight tier-model + clipboard Phase 2~~ | — | ✅ **ALL CLOSED** across Strikes 016 → 021 |
| ~~P0~~ | ~~clipboard RULE-007 §Disclosure block~~ | — | ✅ **CLOSED** in Strike 022 |
| ~~P2~~ | ~~Extract per-extension inline tier-init scripts to shared `lib/options-tier-init.js`~~ | — | ✅ **CLOSED** in Strike 022 (11 extensions consolidated) |

---

## VI. Per-Pillar Readiness Snapshot (post-Strike-021 — Fleet-Wide 100% Milestone)

| Pillar | Active Extensions | Avg readiness rung | Visual-binding compliance | Highest-rung | Lowest-rung |
|---|---|---|---|---|---|
| **OIA / ADHD** (Slate & Sage) | 8 | Rung 3.0 | 🏆 **8 / 8 (100%)** | `864z-chronicle` (Rung 4) | (no sub-Rung-3) |
| **864-Flux** (Slate & Graphite) | 2 | Rung 3.0 ⬆ from 2.0 | 🏆 **2 / 2 (100%)** | `migration-pilot` & `clipboard` ⬆ promoted in Strike 021 | (no sub-Rung-3) |
| **FHG** (Charcoal & Bronze) | 2 | Rung 3.0 | 🏆 **2 / 2 (100%)** | `Bible-Insight` & `scripture-scout` both Rung 3 | (no sub-Rung-3) |

**🏆🏆🏆 Strike-021 FLEET-WIDE 100% Milestone:** all 3 pillars at **100% Rung-3+ visual-binding compliance**. **Active fleet: 12 / 12 (100%)** on every primary axis: RULE-001 + RULE-006 v1.1 + SW `type: "module"` + Tier-0.5 Rung-3+ + canonical-ID visual binding. The Tier-0.5 readiness ladder is no longer a per-extension shortlist; it is now a fleet-wide property. The next ladder advance is Rung 3 → Rung 4 (paywall + DEV_NOTES) — that's a per-extension upgrade with cumulative effort but no longer a structural compliance gap.

---

## VII. Cross-References

- Programmatic source data: `extensions/*/manifest.json` (15 files) + `extensions/*/_locales/*/messages.json` + `extensions/*/options/*` (8 options pages) + `extensions/*/lib/*` (per-extension copies of design-system + transparency-tier).
- Canonical CSS: [`864z-build-kit/references/core/transparency-tier.css`](../../864zeros-llc/864z-build-kit/references/core/transparency-tier.css) — the visual contract that all 9 options-bearing extensions reference.
- Tier-0.5 visual spec: [`extensions/864z-chronicle/TIER_0_5_BLUEPRINT.md`](../../864zeros-llc/LLC-DIV-3-FACTORY/extensions/864z-chronicle/TIER_0_5_BLUEPRINT.md).
- Sovereign Link spec: [`extensions/864z-chronicle/SOVEREIGN_LINK_PROPOSAL.md`](../../864zeros-llc/LLC-DIV-3-FACTORY/extensions/864z-chronicle/SOVEREIGN_LINK_PROPOSAL.md).
- Dev-override discipline: [`extensions/864z-chronicle/DEV_NOTES.md`](../../864zeros-llc/LLC-DIV-3-FACTORY/extensions/864z-chronicle/DEV_NOTES.md) §I.
- Pillar doctrine: [`864zeros_PILLAR_STRATEGY.md`](./864zeros_PILLAR_STRATEGY.md).
- Per-rule compliance audit: [`864zeros_TECH_STACK_AUDIT.md`](./864zeros_TECH_STACK_AUDIT.md).
- Strategic timing: [`864zeros_2026_ROADMAP.md`](./864zeros_2026_ROADMAP.md).
- Privacy/data-sovereignty audit: [`864zeros_SOVEREIGN_GAP_REPORT.md`](./864zeros_SOVEREIGN_GAP_REPORT.md).
- Active sprint state: [`../ISD-DIV-5-EVOLUTION/BACKLOG.md`](../ISD-DIV-5-EVOLUTION/BACKLOG.md).
- Append-only event ledger: [`../ISD-DIV-5-EVOLUTION/reports/SYSTEM_STRIKE_LOG.md`](../ISD-DIV-5-EVOLUTION/reports/SYSTEM_STRIKE_LOG.md).

---

## VIII. Versioning

| Version | Date | Changes |
|---|---|---|
| 1.0 | 2026-05-09 | Initial. Programmatic survey of all 15 extensions across 3 pillars (OIA 11 · 864-Flux 2 · FHG 2). Tier-0.5 readiness ladder (Rung 0 → Rung 4) codified with per-rung effort estimates. RULE-006 v1.1 fleet compliance: 15/15 (Zero-Point Audit verified). 1 extension on Rung 4 (chronicle); 2 on Rung 2; 6 on Rung 1; 6 on Rung 0 (BLOCKED on RULE-001). Recommended strike sequence published. |
| 1.1 | 2026-05-09 | Post-Strikes-016+017 update. **Active fleet: 12** (3 archived: `oia.focus.signal/sound`, `oia-focus-timer`). TabVault → DataNap rebrand. 6 OIA extensions promoted Rung 0/1 → Rung 2-3 via Strike 016. who-is-watching promoted Rung 0 → Rung 3 via Strike 017 (also closes SW `type: "module"` modernization gap). **100% fleet compliance milestone: RULE-001 + RULE-006 v1.1 + SW type:module all at 12/12.** Strike sequence updated: P0 batch RULE-001 scaffold ✅ CLOSED; new P0 = Bible-Insight RULE-007 audit + lib/tier.js distribution. Per-pillar avg rung shift: OIA 1.0 → 3.0; 864-Flux unchanged (Phase-2 deferred); FHG 1.5 → 2.0. |
| 1.2 | 2026-05-09 | Post-Strike-018 update. **Bible-Insight promoted Rung 2 → Rung 3** (full state machine + dev gate). Bible-Insight per-extension RULE-007 audit doc delivered (verdict: structurally compliant; BYOK Gemini, debugger bounded to PDF generation, no proxy; one outstanding §Disclosure UX gap = new P0). migration-pilot + scripture-scout `tier-card--upcoming` → `tier-card--locked` HTML alias (matches Chronicle Standard); dead local CSS rules replaced with marker comments. Rung 3+ cohort grew 8 → 9 (75% of active fleet). FHG avg rung 2.0 → 2.5 (Bible-Insight at Rung 3; scripture-scout still Rung 2 pending lib/tier.js). New P1 MICRO: copy lib/tier.js + inline tier-init script to migration-pilot + scripture-scout (~10 min batched) — would bring active fleet to 11/12 on Rung 3+. |
| 1.3 | 2026-05-09 | Post-Strike-019 update. **Three sub-strike deliverables:** (1) Bible-Insight RULE-007 §Disclosure block injected before brand-footer (verbatim from audit doc §V.a) — closes the one outstanding P1 from Strike 018 audit; (2) `lib/tier.js` distributed to migration-pilot + scripture-scout + dev-override panel + inline `<script type='module'>` tier-init injected — both promoted Rung 2 → Rung 3; (3) NEW operational artifacts at LLC-DIV-3-FACTORY/ root: `FACTORY_LEDGER.jsonl` (machine-readable JSON-per-line audit stream) + `SESSION_STREAM.md` (human-readable companion) — append-only audit trail of every atomic factory mutation. **Rung 3+ cohort grew 9 → 11 (92% of active fleet)** — only `clipboard` remains on Rung 1 (Phase-2 HIGH-deferred). 864-Flux avg rung 1.5 → 2.0; FHG avg rung 2.5 → 3.0 (now full uniformity at Rung 3). Honest defect: 2 ledger entries were written prematurely (in parallel with failed Edits requiring Read-first); correction entry appended honoring CLAUDE-INTEGRITY. |
| 1.4 | 2026-05-09 | Post-Strike-020 update. **🏆 Visual-compliance milestone**: OIA (ADHD) and FHG pillars are now **100% Rung-3 visual-binding compliant** — every options page in those pillars (10 of 10 active OIA+FHG extensions) has the canonical `vault-tier-card` / `current-tier-name` / `vault-lock-watermark` IDs that the inline tier-init script targets; visual state transitions on tier-flip work end-to-end. 864-Flux is at 1/2 (clipboard pending Phase 2). **Bible-Insight tier rebrand**: "⌖ Tier-0.5: Vault" → "⌖ Sovereign Research Kit" per Operator GTM decision; $2.99 perpetual unlock model (Chronicle pattern); closes Strike-018 P0 GTM-decision. 5-feature list expanded; "why $2.99 once" rationale added. Internal tier flag name unchanged (TIER_VAULT) for cross-extension code consistency. **Strike Sequence**: 6 prior items marked CLOSED; new P0 = Clipboard Phase 2 (now the LAST sub-Rung-3 extension); new P2 = Bible-Insight ExtPay integration for Sovereign Research Kit. |
| 1.5 | 2026-05-09 | Post-Strike-021 — **🏆🏆🏆 FLEET-WIDE 100% RUNG-3 MILESTONE.** clipboard Phase-2 closure: prior strikes had already given clipboard the [864F] prefix + SW type:module + brand-footer + linked transparency-tier.css; Strike 021 added the missing pieces (`lib/tier.js`, NEW Sovereign History tier-card with canonical IDs, dev-override panel + inline tier-init, `id="current-tier-name"` on existing tier-badge). Per-extension `RULE_007_AUDIT.md` delivered (~16 KB; 8 sections covering BYOK Gemini + debugger + chrome.identity Google OAuth + ExtPay 3rd-party + chrome.management; verdict: structurally compliant; one P1 §Disclosure UX gap pending). **Active 12-extension fleet: 12/12 (100%) Rung 3+ AND 12/12 (100%) visual-binding compliant. All 3 pillars (OIA/ADHD + 864-Flux + FHG) at 100% Rung-3 visual-compliance.** The Tier-0.5 readiness ladder is now a fleet-wide property; Rung 0/1/2 buckets are empty. Per-pillar avg rung: OIA 3.0 (unchanged) · 864-Flux 2.0 → 3.0 · FHG 3.0 (unchanged). Strike Sequence: ALL prior P0 items CLOSED across Strikes 016-021; new P0 = clipboard RULE-007 §Disclosure block injection (~30 min). |
| 1.6 | 2026-05-09 | Post-Strike-022 — **CONSOLIDATION + PAYMENT-ARCHITECTURE SPEC.** Three deliverables: (1) clipboard RULE-007 §Disclosure block injected (verbatim from audit §VI.a; covers all 5 high-trust surfaces); closes Strike-021 P1. (2) Shared `lib/options-tier-init.js` extracted from per-extension inline scripts; canonical at `864z-build-kit/references/core/options-tier-init.js`; distributed to 11 extensions (chronicle excluded — uses `options/options.js` not inline; queued as P1 follow-up). 11 inline `<script type="module">` blocks replaced with `<script src="../lib/options-tier-init.js">` linkage; eliminates ~80 LOC of cross-extension duplication. (3) Chronicle Checkout Blueprint authored (`extensions/864z-chronicle/CHRONICLE_CHECKOUT_BLUEPRINT.md`, ~15 KB / 8 sections). Identifies 3 ExtPay entry points (SW initPayments+onPaid, Options page CTA swap, shared TIER_UNLOCKED listener), 6 operator pre-integration checklist items, 6 failure modes + mitigations, generalization path for the other 11 Rung-3 extensions. Strike Sequence: 3 prior items marked CLOSED (clipboard §Disclosure, shared options-tier-init.js extraction, RULE-008 strike sequence cleanup); new P0 = Chronicle ExtPay implementation per blueprint (~3-4h); new P1 = chronicle migration to shared options-tier-init.js (~30 min); new P1 = Bible-Insight + clipboard ExtPay replication after Chronicle proves the pattern. Active fleet readiness unchanged at 12/12 Rung 3+ (Strike 022 was consolidation + spec, not a Rung promotion). |
| 1.65 | 2026-05-09 EOD | Post-Strike-023 EOD wrap-up — **COMPLIANCE-TO-REVENUE PIVOT.** Three deliverables: (1) FACTORY_LEDGER.jsonl + SESSION_STREAM.md audit verified — 39 entries (38 from Strike 019-022 arc + 1 from Strike 023 init); 39/39 valid JSON; 39/39 schema-complete (all 7 required fields); timestamps monotonic; stream/ledger 1:1 correspondence intact. (2) §V Strike Sequence reorganized: **Chronicle ExtPay Integration moved to ABSOLUTE TOP of P0 queue** (🔥 P0-TOP) with "TOMORROW MORNING — START HERE" annotation; first revenue-generating strike. (3) NEW EOD_LOG.md at `LLC-DIV-3-FACTORY/EOD_LOG.md` summarizing today's compliance-to-revenue pivot (Strike 016 → 022 = 7 strikes closing all Rung-0/1/2 gaps + 100% fleet milestone + payment blueprint). No Rung promotions in Strike 023 (audit + sequence reorganization only); active fleet remains 12/12 Rung 3+. |
| 1.9 | 2026-05-11 | Post-Strike-029 — **🔐 TRUST VAULT UI FLEET ROLLOUT.** Four deliverables: (1) Canonical `lib/options-tier-init.js` gains `injectTrustVaultUI()` — renders operator-mandated UI into `#trust-vault-root` elements with 5 byte-exact operator-verbatim strings (Header `864zeros Trust Vault`; Status `Privacy Status: Local-Only (No Data Stored by 864zeros)`; 2 button labels; full Privacy Guarantee block). Idempotent. Default button wiring: Export = `chrome.storage.local.get(null)` → exportVault → `.md` download; Import = importVault (Strike-028 alert) → `clear()` + `set()` OVERWRITE → re-render tier/dev/brand-mission. (2) Trust Vault library `lib/trust-vault.js` distributed from chronicle-only (Strike 028) to all 12 active extensions — fleet-wide parity. (3) 24-file fleet redistribution: both options-tier-init.js (sha `ad42cff6...`) AND trust-vault.js (sha `935bc37a...`) to all 12 extensions' `lib/`. 24/24 SHA-identical, 24/24 module-mode syntax-clean. (4) 12 options.html files gain `<div id="trust-vault-root"></div>` immediately before the standardized brand-footer (Python script handled 3 anchor-pattern variants — plain `brand-footer` vs `brand-footer oia-mt-md` — plus Bible-Insight's non-standard `html/options.html` path). 12/12 final verification. **Fleet-wide Trust Vault parity: 0/12 → 12/12.** No Rung promotions (UI rollout, not Rung advance); active fleet remains 12/12 Rung 3+ with chronicle on Rung 4: Active Checkout. Strike 028's library activation strike: shipped. |
| 1.85 | 2026-05-11 | Post-Strike-027 — **🛡 PAYMENT-PERSISTENCE REPAIR + 4TH DEFENSE LAYER.** Three deliverables: (1) `extpay-wrapper.js initPayments()` refactored to accept an optional onPaidCallback and register the listener BEFORE `extpay.startBackground()` — closes the race window noted (but not fixed) in Strike 026 diligence where the first poll could fire before a separately-deferred addListener landed (would cause missed payment-confirmation on SW restart). Exported `onPaid()` retained for backwards-compatibility. (2) SW switched to single-call pattern `initPayments(async (user) => {...})` — one import, one call site, race structurally eliminated. (3) NEW canonical fail-safe `tryExtPaySync()` in `lib/options-tier-init.js` — dynamic-imports per-extension wrapper (graceful no-op for the 11 without one); calls getCurrentTier; upgrade-only ExtPay-as-source-of-truth sync (downgrade per blueprint §V FM4 is its own concern); wired on page init AND window focus. Redistributed to all 12 extensions (12/12 SHA-identical + module-mode syntax-clean). Defense-in-depth payment-confirmation chain now 4 layers (SW onPaid → runtime broadcast → focus listener → ExtPay sync) + chronicle's bonus storage.onChanged layer. No Rung promotions (persistence + safety-net work); active fleet remains 12/12 Rung 3+ with chronicle on Rung 4: Active Checkout. |
| 1.8 | 2026-05-11 | Post-Strike-026 — **🔧 CHRONICLE PAYMENT-FLOW DEBUG + UX HARDENING.** Five deliverables: (1) Chronicle SW `onPaid()` handler instrumented with 4-point diagnostic logging + try/catch around setTier (early return on persistence failure) + try/catch around sendMessage (no-listener case not fatal). (2) Best-effort "redirect" to options.html on payment success via storage-passed tab ID (no `tabs` permission added; options.js writes its own tab ID to `chrome.storage.local.paymentReturnTabId` before checkout; SW reads + chrome.tabs.update + chrome.windows.update + one-shot remove). ExtPay SDK confirmed to have no native redirect param. (3) Canonical `lib/options-tier-init.js` gains `window.addEventListener('focus')` that re-renders tier card; redistributed to all 12 extensions (12/12 SHA-identical + module-mode syntax-clean). (4) Chronicle options.js TIER_UNLOCKED listener now shows toast('Payment Successful! Vault unlocked.', 5000); TIER_DOWNGRADED branch separately shows downgrade toast. (5) Three-layer defense for "user returns and sees new tier" UX: SW broadcast (active tab) + shared focus listener (background tab) + chronicle storage.onChanged (catch-all on storage write). No Rung promotions (debug + UX work); active fleet remains 12/12 Rung 3+ with chronicle on Rung 4: Active Checkout. |
| 1.75 | 2026-05-11 | Post-Strike-025 — **🎯 CROSS-SURFACE BRAND_MISSION INSTALLATION.** Five deliverables: (1) NEW canonical `864z-build-kit/references/core/brand-identity.js` (exports `BRAND_MISSION` — founder-voice mission statement, operator-verbatim 4-sentence text with em-dash). (2) Canonical `lib/options-tier-init.js` updated to import + inject BRAND_MISSION into `#brand-mission` elements (defensive null-check; pages without the div no-op). (3) 24-file fleet-wide redistribution: both files copied to all 12 active extensions' `lib/` directories — 24/24 SHA-identical to canonicals, 24/24 module-mode syntax-clean. (4) Chronicle's `options/options.html` gains `<div id="brand-mission" class="brand-text"></div>` above the brand-footer (first extension to render BRAND_MISSION). (5) NEW canonical template at `ISD-DIV-5-EVOLUTION/templates/GTM_BUILD_REPORT_TEMPLATE.md` — fulfills the per-strike Build Report mandate from `ROLES/OFFICE_ARCHITECT.md` §IV; BRAND_MISSION as blockquote header; 8 RULE-008-compliant sections (Thesis · Target Customer · Source Liberation Targets · Pricing Tier · Hook Copy · Privacy/RULE-007 Disclosure · Cross-References · Versioning). No Rung promotions (branding/copy work); active fleet remains 12/12 Rung 3+ with chronicle on Rung 4: Active Checkout. |
| 1.7 | 2026-05-10 | Post-Strike-024 — **🚀 REVENUE ARC OPENED — first real-payment extension live.** Six deliverables per Operator's 5-task directive: (1) NEW gitignored `extensions/864z-chronicle/js/config.js` with operator-verbatim values: `EXTPAY_ID='chronicle'`, `SOVEREIGN_PRICE_ID='price_1TVXVk3O3iJB3kbAdWYsldTz'`, `PLAN_ID='one-time-tier-05'`; only EXTPAY_ID flows into the SDK call (other two are operator metadata for Stripe-dashboard cross-reference). (2) **Chronicle ExtPay integration shipped per `CHRONICLE_CHECKOUT_BLUEPRINT.md`** — all 3 entry points: SW `initPayments + onPaid` handler (sets TIER_VAULT + broadcasts TIER_UNLOCKED on payment); Options `onUnlockVault` swap from Strike-013 stub to `openPaymentPage()`; shared `TIER_UNLOCKED`/`TIER_DOWNGRADED` listener appended to canonical `lib/options-tier-init.js`. NEW `lib/payments/{ExtPay.js, extpay-wrapper.js}` (chronicle-specific minimal binary-tier wrapper). manifest.json gains `content_scripts` for `extensionpay.com/*` per ExtPay's setup pattern. (3) **Chronicle migrated to shared `lib/options-tier-init.js`** (closes Strike-022 P1 — chronicle was the lone holdout). `initDevOverride()` + `refreshDevTierLabel()` deleted from `options.js` (handled by shared); `renderTierUI()` retained for chronicle-extras (description text + markdown button); two new listeners added (`chrome.runtime.onMessage` + `chrome.storage.onChanged`). 418 LOC final (down from 443; net -25). 12/12 fleet now uses shared tier-init. (4) **Vendored canonical `ExtPay.js`** (52 KB / 1578 LOC) to `864z-build-kit/references/core/payments/` — single source of truth for the SDK across the fleet (closes Strike-022 P2). 3 SHA-identical copies: clipboard's existing + canonical + chronicle's new. (5) Updated canonical `lib/options-tier-init.js` with TIER_UNLOCKED/TIER_DOWNGRADED listener + redistributed to all 12 per-extension copies (12/12 SHA-identical post-distribution). (6) §V Strike Sequence rotated: 3 prior items marked CLOSED (Chronicle ExtPay impl, chronicle shared-script migration, vendor ExtPay.js); new P0-TOP = Bible-Insight + clipboard ExtPay replication (~1h batched per blueprint §VI). **Chronicle promoted Rung 4 (stub) → Rung 4 (Active Checkout) — first revenue-generating extension live.** §IV.e introduces sub-rung distinction (stub vs Active Checkout). Active fleet remains 12/12 Rung 3+ (within-rung promotion, not a new tier). |

---

*864zeros Factory Manifest v1.9 · 2026-05-11 · 864zeros LLC · DIV-6-KNOWLEDGE.*
