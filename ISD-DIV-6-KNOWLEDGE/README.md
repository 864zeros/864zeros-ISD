# ISD-DIV-6-KNOWLEDGE — AI Ingestion Layer (NotebookLM / RAG)
**Role:** Curated, atomic context documents optimized for NotebookLM and RAG ingestion. Every other division feeds DIV-6; AI agents read FROM DIV-6, never directly from raw division files.
**Pillars served:** All three (Master Context names the Brand Firewall verbatim — OIA / 864-Flux / FHG).
**Source of truth:** `64zeros_MASTER_CONTEXT.md.md` — the strategic master context document. **(Operator note: filename has a typo — should be `864zeros_MASTER_CONTEXT.md`; rename queued for next cleanup pass to avoid breaking existing NotebookLM ingestion until that re-index runs.)**
**Governing rules:** RULE-000 (Master Context updates are an Architect-signed activity; bulk additions require strike charter to keep ingestion deterministic). All cited rules: RULE-000 through RULE-007.
